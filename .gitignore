# Node (dist/ IS committed — consumers install from git URL without a build step)
node_modules/
npm-debug.log*

# macOS
.DS_Store

# Local config (each consumer project commits its own afk.config.json)
.afk-state.json
.afk/events.jsonl

# Editor
.vscode/
.idea/
